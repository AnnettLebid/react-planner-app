import React from 'react'

const Subheader = (props) => {
    return (
        <div className = "sub-header">
            <h4>{props.name}</h4>
            
        </div>
    )
}

export default Subheader;